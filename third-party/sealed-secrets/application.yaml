# See the below links for more information:
#   - https://argo-cd.readthedocs.io/en/stable/user-guide/application-specification/
#   - https://argo-cd.readthedocs.io/en/stable/operator-manual/cluster-bootstrapping/
#   - https://argo-cd.readthedocs.io/en/stable/user-guide/multiple_sources/
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sealed-secrets
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: pks-bootstrapping
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  finalizers:
    - resources-finalizer.argocd.argoproj.io/background
spec:
  project: admin-proj
  sources:
    - repoURL: https://bitnami-labs.github.io/sealed-secrets
      targetRevision: 2.17.7
      chart: sealed-secrets
      helm:
        releaseName: sealed-secrets
        valueFiles:
          - $values/third-party/sealed-secrets/values.yaml
    - repoURL: https://github.com/PoolC/pks-bootstrapping.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      enabled: true
      prune: true
      # Disable self-heal to enable faster debugging within the cluster.
      selfHeal: false
    syncOptions:
      - ApplyOutOfSyncOnly=true
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  info:
    - name: "Sealed Secrets GitHub Repository:"
      value: https://github.com/bitnami-labs/sealed-secrets
